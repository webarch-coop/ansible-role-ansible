---
argument_specs:
  main:
    author: Chris Croome
    description: An Ansible role for installing Ansible.
    short_description: The main entry point for the Ansible role.
    options:
      ans:
        type: bool
        required: true
        description: Run the tasks in this role.
      ans_collections:
        type: list
        required: true
        description: Ansible collections to install.
        elements: dict
        options:
          name:
            type: str
            required: true
            description: The name of the collection to install.
          version:
            type: str
            required: true
            description: The version of the collection to install.
      ans_version:
        type: str
        required: true
        description: The version of Ansible that will be installed if the packaged version is less than this version.
...

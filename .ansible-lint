---
# https://ansible-lint.readthedocs.io/rules/
skip_list:
  # You can improve the task key order
  - key-order[task]
  # Commands should not change things if nothing needs doing
  - no-changed-when
# vim: syntax=yaml
...

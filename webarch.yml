---
- name: Webarchitects Debian Server Config
  hosts: "{{ hostname }}"
  become: yes

  vars:
    distro: stretch

  roles:
    - bash
    - apt
    - ssh
    - vim
    - locale
    - munin-node
    - chrony
    - rsync
    - screen
    - mutt
    - fail2ban
    - lynis
